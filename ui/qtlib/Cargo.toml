[package]
name = "qtlib"
version = "0.1.0"
authors = ["ZecWallet"]
edition = "2018"

[lib]
name = "zecpaperrust"
crate-type = ["staticlib"]

[dependencies]
libc = "0.2.58"
zecpaperlib = { path = "../../lib" }